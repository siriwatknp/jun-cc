# Composite

Internal keyboard navigation utility -- not a public component.

Used by: Tabs, Menu, Select, Combobox, RadioGroup, ToggleGroup, Toolbar.

## Review Checklist

❌ `import { Composite } from '@base-ui/react/composite'` -- internal only, not for direct use
❌ Custom keyboard navigation reimplemented on top of Base UI list components -- already built-in via Composite
